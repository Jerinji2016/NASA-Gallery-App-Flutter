import 'package:flutter/material.dart';

class ImageDetailsView extends StatelessWidget {
  const ImageDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
